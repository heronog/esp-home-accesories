packages:
  secrets: !include secrets.yaml

wifi:
  ssid: ${wifi_ssid}
  password: ${wifi_password}

  # Enable fallback hotspot (captive portal) in case wifi connection fails
  ap:
    ssid: ${device_uppercase} Hotspot
    password: ${hotspot_password}

captive_portal:
